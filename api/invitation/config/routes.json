{
  "routes": [
    {
      "method": "PUT",
      "path": "/invitations/",
      "handler": "Invitation.updateByInviteCode",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/invitations/",
      "handler": "Invitation.retrieveGuestsByInviteCode",
      "config": {
        "policies": []
      }
    }
  ]
}
